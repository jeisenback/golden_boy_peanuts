name: Backtests prototype

<<<<<<< HEAD
permissions:
  contents: read

=======
>>>>>>> d82f7c7 (Backtest: add robust alignment, event CSV export, optional plotting; add unit test and CI workflow)
on:
  pull_request:
    paths:
      name: backtests

      on:
        push:
          branches: [develop]
          paths:
            - 'backtests/**'
        pull_request:
          branches: [develop]
          paths:
            - 'backtests/**'

      permissions:
        contents: read

      jobs:
        backtests:
          runs-on: ubuntu-latest
          steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Set up Python
              uses: actions/setup-python@v4
              with:
                python-version: '3.11'

            - name: Install deps
              run: |
                python -m pip install --upgrade pip
                pip install -r requirements-dev.txt
                pip install pydantic typing-extensions

            - name: Run backtest tests
              run: pytest -q tests/test_backtest_gdelt_vol.py -q
